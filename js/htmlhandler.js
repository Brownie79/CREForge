//imports
    const fs = require('fs');

module.exports = {
    building : function(id, response){
        //fetch building data based on database
    },
    renter : function(id, response){
        //fetch building data based on database
    }
}